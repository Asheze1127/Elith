// Placeholder root page. app/ stays routing-only: the real chat UI is a
// Container component mounted here, implemented in #15.
export default function Home() {
  return (
    <main style={{ padding: "2rem" }}>
      <h1>Elith</h1>
      <p>Chat UI scaffold — implemented in a follow-up issue.</p>
    </main>
  );
}
