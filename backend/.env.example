# Environment config (app-wide startup conditions). Copy to .env and adjust.
# local -> Gemini free API / no key -> mock
ENVIRONMENT=local
DATABASE_URL=postgresql+psycopg://elith:elith@db:5432/elith
GEMINI_API_KEY=
LOG_LEVEL=INFO
